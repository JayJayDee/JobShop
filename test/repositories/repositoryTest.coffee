
_ = require('underscore') 
sinon = require('sinon')

describe('is repository method all exists', () ->

  # before test, insert a mock DB
  # prevent calling DB
  before(() ->

  )
)

describe('enqueue', () ->
  
) 